<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Impact.HomePage"
             Title="Home" IconImageSource="outline_house_black_18dp.png">
    <ContentPage.Content>
        <StackLayout Spacing="10" Padding="10">
            <ListView x:Name="listHomePosts" ItemsSource="{Binding HomePosts}" HasUnevenRows="True" ItemTapped="OnItemSelected" SeparatorVisibility="None" IsPullToRefreshEnabled="True">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ImageCell Text="{Binding posterName}" TextColor="Orange" Detail="{Binding title}" DetailColor="Black" ImageSource="{Binding universityImageUrl}"/>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>