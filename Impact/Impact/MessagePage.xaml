<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Impact.MessagePage"
             Title="Messages" IconImageSource="outline_chat_black_18dp.png">
    <ContentPage.Content>
        <FlexLayout Margin="1">
            <StackLayout Spacing="3">
                <ListView x:Name="listMessages" ItemsSource="{Binding Messages}" HasUnevenRows="True" ItemTapped="OnItemSelected" SeparatorVisibility="None" IsPullToRefreshEnabled="True">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <ImageCell Text="{Binding userName}" TextColor="Orange" Detail="{Binding lastText}" DetailColor="Black" ImageSource="{Binding universityImageUrl}"/>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackLayout>
        </FlexLayout>
    </ContentPage.Content>
</ContentPage>